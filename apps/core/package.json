{
  "name": "@jarvis/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --no-warnings=ExperimentalWarning --watch --import tsx src/server.ts",
    "start": "node --no-warnings=ExperimentalWarning --import tsx src/server.ts",
    "typecheck": "tsc -p tsconfig.json",
    "test": "node --test --import tsx test/*.test.ts",
    "bench:ttft": "tsx scripts/bench-ttft.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.122.0",
    "@fastify/cors": "^11.3.0",
    "@fastify/websocket": "^11.3.0",
    "@jarvis/shared": "*",
    "dotenv": "^17.4.2",
    "fastify": "^5.12.1",
    "zod": "^4.5.2"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "@types/ws": "^8.18.1",
    "tsx": "^4.23.12",
    "typescript": "~5.9.3"
  }
}
